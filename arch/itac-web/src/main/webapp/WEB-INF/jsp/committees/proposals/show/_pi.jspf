<!-- DEAD CODE RIGHT NOW -->							<div id="primaryInvestigator">
								<label>First name:</label>${ proposal.document.common.investigators.primaryInvestigator.firstName }<br/>
								<label>Last name:</label>${ proposal.document.common.investigators.primaryInvestigator.lastName }<br/>
								<label>Visiting:</label>${ proposal.document.common.investigators.primaryInvestigator.visiting }<br/>
								<label>Status:</label>${ proposal.document.common.investigators.primaryInvestigator.status.name }<br/>
								<div id="piContact">
									<label>Email:</label>
									<ul>
									<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.contact.email }" var="item">
										<li>${ item }</li>
									</c:forEach>
									</ul>
									<label>Fax:</label>
									<ul>
									<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.contact.fax }" var="item">
										<li>${ item }</li>
									</c:forEach>
									</ul>
									<label>Phone:</label>
									<ul>
									<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.contact.phone }" var="item">
										<li>${ item }</li>
									</c:forEach>
									</ul>
								</div>
								<div id="piSite">
									<h7>PI Site</h7><br/>
									<label>Country:</label>${ proposal.document.common.investigators.primaryInvestigator.site.country }<br/>
									<label>Institution:</label>${ proposal.document.common.investigators.primaryInvestigator.site.institution }<br/>
									<label>Address:</label>
									<ul>
									<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.site.address }" var="item">
										<li>${ item }</li>
									</c:forEach>
									</ul>
									<div id="piSiteContact">
										<label>Email:</label>
										<ul>
										<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.site.contact.email }" var="item">
											<li>${ item }</li>
										</c:forEach>
										</ul>
										<label>Fax:</label>
										<ul>
										<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.site.contact.fax }" var="item">
											<li>${ item }</li>
										</c:forEach>
										</ul>
										<label>Phone:</label>
										<ul>
										<c:forEach items="${ proposal.document.common.investigators.primaryInvestigator.site.contact.phone }" var="item">
											<li>${ item }</li>
										</c:forEach>
										</ul>
									</div>
								</div>
							</div>